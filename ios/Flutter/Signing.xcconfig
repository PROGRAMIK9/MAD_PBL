// Copy this file locally if you need to override signing in custom build setups.
// Xcode signing is still configured in the Runner target, but this file documents
// the expected release distribution settings.

CODE_SIGN_STYLE = Automatic
DEVELOPMENT_TEAM = YOUR_TEAM_ID
PRODUCT_BUNDLE_IDENTIFIER = com.example.offlineMeshApp
PROVISIONING_PROFILE_SPECIFIER =